import styled from 'styled-components';

const Wrapper = styled.div`
  border-bottom: 10px solid var(--the-primary-color);
  padding: 1rem;
  color: var(--text-color);
  text-align: center;
  h3 {
    font-size: 1rem;
  }
  .order-item-order-item {
    /* text-align: center; */
  }
`;
export default Wrapper;
